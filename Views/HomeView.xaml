<UserControl x:Class="GestionnaireInventaire.Views.HomeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GestionnaireInventaire.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Message de bienvenue -->
        <Border Grid.Row="0" 
                Background="#30FFFFFF" 
                CornerRadius="10" 
                Margin="0,0,0,20" 
                Padding="20">
            <StackPanel>
                <TextBlock Text="Bienvenue dans votre gestionnaire d'inventaire" 
                           Foreground="{StaticResource lightBrush}"
                           FontSize="24" 
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,10"/>
            </StackPanel>
        </Border>

        <!-- Image d'accueil -->
        <Image Grid.Row="1" Source="/Images/home.png" Stretch="Uniform"/>
    </Grid>
</UserControl>